# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
.env
.env.*

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Generated by scripts/aggregate.ts — contains real machine data.
# live-data.example.json is the committed sanitized template.
src/data/live-data.json

# Argus — personal watchlist (handles, accounts, keywords) generated by
# `bun run install-argus`. The committed sanitized templates are
# config/argus-watchlist.*.example.json
config/argus-watchlist.json
# Argus runtime state lives outside the repo at ~/.hermes/argus/ — never committed

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
claude-os
